# 🌐 رفع «علامة» على الإنترنت

بعد هذه الخطوات (تُنجَز مرة واحدة، وتستغرق نحو ١٥ دقيقة) سيكون لتطبيقك
**رابط دائم** مثل `aisha.pythonanywhere.com` يفتحه طلابك من أي جهاز وأي مكان،
وبياناتهم محفوظة دائمًا — بلا تشغيل أي شيء على جهازك بعدها أبدًا.

الخدمة **مجانية تمامًا ولا تطلب بطاقة بنكية**.

---

## ١. أنشئي حسابًا

1. افتحي **pythonanywhere.com**
2. اضغطي **Pricing & signup** ثم **Create a Beginner account** (المجاني).
3. اختاري اسم مستخدم بحروف إنجليزية — **هذا الاسم سيصبح رابط تطبيقك**،
   فاختاريه بعناية: `aisha` ينتج `aisha.pythonanywhere.com`.
4. أكّدي بريدك الإلكتروني.

---

## ٢. ارفعي ملفات التطبيق

1. من الشريط العلوي اضغطي تبويب **Files**.
2. في قسم **Files** اضغطي زر **Upload a file**.
3. اختاري الملف **`imla-upload.zip`** الموجود في مجلد التطبيق على سطح مكتبك.
4. انتظري حتى يكتمل الرفع.

---

## ٣. فكّي ضغط الملفات

1. من الشريط العلوي اضغطي تبويب **Consoles**.
2. اضغطي **Bash** (ستفتح نافذة سوداء تكتبين فيها).
3. اكتبي هذا السطر ثم اضغطي Enter:

```
unzip -o imla-upload.zip -d imla
```

4. للتأكد أن كل شيء وصل، اكتبي:

```
ls imla
```

يجب أن ترى: `app.py  content.py  engine.py  server.py  store.py  static`

---

## ٤. أنشئي تطبيق الويب

1. اضغطي تبويب **Web** ثم **Add a new web app**.
2. اضغطي **Next** (سيخبرك أن رابطك سيكون `اسمك.pythonanywhere.com`).
3. اختاري **Manual configuration** — ⚠️ **ليس** Flask ولا Django.
4. اختاري **Python 3.10** (أو أي إصدار ٣.١٠ فأعلى) ثم **Next**.

---

## ٥. اربطي التطبيق

1. ستفتح صفحة إعدادات طويلة. انزلي إلى قسم **Code**.
2. اضغطي على الرابط الأزرق بجوار **WSGI configuration file**
   (يبدو مثل `/var/www/اسمك_pythonanywhere_com_wsgi.py`).
3. سيفتح محرّر نصوص. **امسحي كل ما فيه** (Ctrl+A ثم Delete).
4. افتحي ملف **`pythonanywhere_wsgi.py`** من مجلد التطبيق على سطح مكتبك،
   وانسخي محتواه كاملاً، والصقيه في المحرّر.
5. اضغطي زر **Save** الأخضر في الأعلى.

---

## ٦. شغّليه

1. ارجعي إلى تبويب **Web**.
2. اضغطي الزر الأخضر الكبير **Reload**.
3. افتحي رابطك: **`اسمك.pythonanywhere.com`**

يجب أن تظهر شاشة «اكتب رقمك». 🎉

---

## ٧. جهّزي صفّك

1. في التطبيق اضغطي **«أنا المعلّمة»** ← **«ليس لديّ حساب — إنشاء حساب»**.
2. سجّلي بريدك وكلمة مرور (٨ أحرف فأكثر).
3. أضيفي أسماء طلابك واحدًا واحدًا. اتركي خانة الرقم فارغة ليختار التطبيق
   رقمًا غير مكرّر، أو اكتبي رقمًا من ٤ أرقام تختارينه أنتِ.
4. أعطي كل طالب رقمه — يكتبه مرة واحدة، ثم يتذكّره جهازه بعد ذلك.

---

## أمور مهمة

**تجديد الحساب المجاني** — كل ٣ أشهر ترسل لك PythonAnywhere بريدًا يطلب
الضغط على زر **Run until 3 months from today** في تبويب Web. إن لم تضغطيه
يتوقف الرابط (البيانات تبقى سليمة، ويعود بالضغط على الزر).

**النسخ الاحتياطي** — كل بيانات الصف في ملف `imla/imla.db` داخل حسابك.
لتحميل نسخة: تبويب **Files** ← ادخلي مجلد `imla` ← اضغطي أيقونة التحميل
بجوار `imla.db`. افعليها آخر كل فصل.

**تحديث التطبيق لاحقًا** — ارفعي الملفات الجديدة بنفس الطريقة، ثم اضغطي
**Reload** في تبويب Web. لا تحذفي `imla.db` وإلا فقدتِ بيانات الطلاب.

**المزايا الاختيارية** (تصحيح الصور والنطق العصبي) لا تعمل على الحساب
المجاني، لأنه يمنع الاتصال بخدمات خارجية. التطبيق يعمل كاملاً بدونها.
