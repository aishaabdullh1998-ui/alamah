# 📦 نشر «علامة» على GitHub

المستودع **جاهز ومحفوظ محليًا بالفعل** — كل الملفات مضافة في أول لقطة (commit).
لم يبقَ إلا ربطه بحسابك على GitHub ودفعه، وهذه خطوات تحتاج تسجيل دخولك أنتِ.

> **ملاحظة مهمة**: `imla.db` (بيانات طلابك) **مستبعد تلقائيًا** ولن يُرفع أبدًا.

---

## ١. أنشئي حسابًا ومستودعًا

1. افتحي **github.com** وسجّلي حسابًا مجانيًا (إن لم يكن لديك).
2. اضغطي **+** في أعلى اليمين ← **New repository**.
3. الاسم: `alamah` (أو أي اسم تحبينه).
4. **اختاري نوع المستودع:**
   - **Private** (خاص) — يراه من تدعينهم فقط. **الأنسب لعمل مدرسي.**
   - **Public** (عام) — يراه أي أحد على الإنترنت. الكود يبقى ملكك (رخصة
     «جميع الحقوق محفوظة» في ملف LICENSE)، لكن أي شخص يستطيع قراءته ونسخه.
5. ⚠️ **لا تضعي علامة** على `Add a README file` ولا `.gitignore` ولا `license` —
   الملفات الثلاثة موجودة عندك بالفعل، وإضافتها هناك تسبب تعارضًا.
6. اضغطي **Create repository**.

---

## ٢. أنشئي مفتاح دخول (Personal Access Token)

GitHub لم يعد يقبل كلمة المرور العادية من الطرفية، بل مفتاحًا خاصًا:

1. من صورتك أعلى اليمين ← **Settings**.
2. انزلي لآخر القائمة اليسرى ← **Developer settings**.
3. **Personal access tokens** ← **Tokens (classic)** ← **Generate new token (classic)**.
4. في **Note** اكتبي: `alamah`
5. في **Expiration** اختاري `90 days` أو `No expiration`.
6. ضعي علامة على **`repo`** فقط (أول خيار، ويشمل ما تحته).
7. اضغطي **Generate token**، ثم **انسخي المفتاح فورًا** — لن يظهر مرة أخرى.

---

## ٣. ادفعي المشروع

افتحي الطرفية ونفّذي هذين الأمرين، بعد استبدال `USERNAME` باسم مستخدمك
و`alamah` باسم المستودع إن غيّرتِه:

```
cd "$HOME/Desktop/تطبيق الاملاد"
git remote add origin https://github.com/USERNAME/alamah.git
git push -u origin main
```

سيسألك:

- **Username**: اسم مستخدمك على GitHub
- **Password**: ⚠️ **الصقي المفتاح** الذي نسختِه في الخطوة ٢ — لا كلمة مرورك.
  (لن تظهر أي حروف أثناء اللصق، هذا طبيعي.)

يحفظ الجهاز المفتاح بعد أول مرة، فلن يسألك مجددًا.

---

## ٤. تحديثات لاحقة

بعد أي تعديل تريدين حفظه على GitHub:

```
cd "$HOME/Desktop/تطبيق الاملاد"
git add -A
git commit -m "وصف مختصر لما غيّرتِه"
git push
```

---

## طريقة بديلة بلا طرفية

إن فضّلتِ تجنّب الأوامر: بعد إنشاء المستودع في الخطوة ١، اضغطي
**uploading an existing file** في صفحته، ثم اسحبي إليها هذه الملفات
والمجلدات من مجلد التطبيق:

```
app.py   content.py   engine.py   server.py   store.py
pythonanywhere_wsgi.py   build-zip.sh
README.md   LICENSE   .gitignore
static/   (المجلد كاملًا)
تشغيل-التطبيق.command
رفع-التطبيق-على-الإنترنت.md
```

⚠️ **لا ترفعي** `imla.db` — فيه بيانات طلابك الحقيقية.
